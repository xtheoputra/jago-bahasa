/* server.js — Entry point. Starts the Jago Bahasa server (static hosting +
   optional auth/sync API). Kept as a thin shim so `node server.js`,
   `npm start`, and START.bat all keep working. Implementation: server/index.js */
require("./server/index.js");
